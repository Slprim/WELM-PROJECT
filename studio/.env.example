# Written by `npx sanity init --env`. Not secret — the project ID is public.
SANITY_STUDIO_PROJECT_ID=
SANITY_STUDIO_DATASET=production

# Editor token, for `npm run seed` only. SECRET — never commit the real value.
# Create at sanity.io/manage -> API -> Tokens.
SANITY_WRITE_TOKEN=
